{
  "private": true,
  "name": "workspace",
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "supabase": "^1.192.5"
  },
  "scripts": {
    "functions:serve": "supabase functions serve"
  }
}
